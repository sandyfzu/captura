[package]
name = "xshot-domain"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true
description = "Domain models for xshot — plain Rust types with no NAPI or platform dependencies"

[dependencies]
thiserror = { workspace = true }
